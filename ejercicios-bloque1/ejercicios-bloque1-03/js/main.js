import { productos } from "./productos.js";
import { calcularPrecioTotal, contarProductos, obtenerNombres, crearTextoResumen } from "./utilidadesProductos.js";
    const cal=calcularPrecioTotal(productos);
    const con=contarProductos(productos);
    const obt=obtenerNombres(productos);
    const cre=crearTextoResumen(productos);

    const a=document.

